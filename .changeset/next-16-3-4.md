---
'@bitrate/web-player': patch
'@bitrate/web-artists': patch
---

Upgraded both Next.js apps to 16.3.4. The release removed `experimental.turbopackMemoryLimit`, which the web player used to cap Turbopack's dev cache; it has no replacement, so disabling the Turbopack filesystem cache is now the only lever there.
