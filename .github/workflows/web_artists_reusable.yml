name: "[web-artists] Reusable"

on:
  workflow_call:
    inputs:
      push-image:
        description: Push docker image
        required: true
        type: boolean
      tags:
        description: Docker tags (multiline)
        required: true
        type: string
      timeout-minutes:
        description: Job timeout in minutes
        required: false
        default: 20
        type: number
      run-biome:
        description: Run biome check before docker build
        required: false
        default: false
        type: boolean
      run-tests:
        description: Run web-artists typecheck and Vitest
        required: false
        default: true
        type: boolean
      run-e2e:
        description: Run Playwright E2E
        required: false
        default: false
        type: boolean
      run-screenshot:
        description: Run Playwright route screenshot regression
        required: false
        default: false
        type: boolean
      cache-to-registry:
        description: Export build cache to GHCR registry
        required: false
        default: true
        type: boolean

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: lordpluha/${{ github.event.repository.name }}

  # `next build` inlines NEXT_PUBLIC_* into the client bundle, so this value is what the
  # published image talks to forever — a server-side env var cannot correct it later. This
  # build passed no build arg at all before, which left the ARG empty and the bundle
  # pointing at apps/web-artists/env.schema.ts's http://localhost:3000 default. The
  # repository declares no Actions variables, so the fallback is the value that applies;
  # `vars.*` stays as the override hook for forks and a future staging environment.
  BUILD_NEXT_PUBLIC_API_URL: ${{ vars.NEXT_PUBLIC_API_URL || 'https://api.bitrate.me' }}

jobs:
  web-artists-checks:
    name: Web Artists Build and Docker
    runs-on: ubuntu-latest
    timeout-minutes: ${{ inputs.timeout-minutes }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js and pnpm
        if: ${{ inputs.run-biome || inputs.run-tests || inputs.run-e2e || inputs.run-screenshot }}
        uses: ./.github/actions/setup-node-pnpm

      - name: Run Biome check
        if: ${{ inputs.run-biome }}
        run: pnpm exec biome ci apps/web-artists packages/ui-react packages/contracts

      # The app typechecks and renders against ui-react's emitted dist/types, not its src,
      # so a missing build fails on components that do exist.
      - name: Build ui-react
        if: ${{ inputs.run-tests || inputs.run-e2e || inputs.run-screenshot }}
        run: pnpm --filter @bitrate/ui-react run build

      - name: Run web-artists typecheck
        if: ${{ inputs.run-tests }}
        run: pnpm --filter @bitrate/web-artists check-types

      - name: Run web-artists unit and integration tests
        if: ${{ inputs.run-tests }}
        run: pnpm --filter @bitrate/web-artists test

      - name: Install Playwright Chromium
        if: ${{ inputs.run-e2e || inputs.run-screenshot }}
        run: pnpm exec playwright install chromium --with-deps

      - name: Run web-artists E2E tests
        if: ${{ inputs.run-e2e }}
        run: pnpm --filter @bitrate/web-artists test:e2e

      - name: Run web-artists screenshot tests
        if: ${{ inputs.run-screenshot }}
        run: pnpm --filter @bitrate/web-artists test:screenshot

      - name: Upload screenshot test failure artifacts
        if: ${{ inputs.run-screenshot && failure() }}
        uses: actions/upload-artifact@v7
        with:
          name: web-artists-screenshot-diffs
          path: apps/web-artists/test-results/
          retention-days: 7

      - name: Setup Docker + registries
        uses: ./.github/actions/setup-docker
        with:
          ghcr: "true"
          ghcr-registry: ${{ env.REGISTRY }}
          ghcr-username: ${{ github.actor }}
          ghcr-password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Web Artists Docker image (with registry cache export)
        if: ${{ inputs.cache-to-registry }}
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./apps/web-artists/Dockerfile
          push: ${{ inputs.push-image }}
          tags: ${{ inputs.tags }}
          cache-from: |
            type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/cache/web-artists:buildcache
            type=gha,scope=web-artists
          cache-to: |
            type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/cache/web-artists:buildcache,mode=max
            type=gha,mode=max,scope=web-artists
          build-args: |
            VITE_API_URL=${{ env.BUILD_NEXT_PUBLIC_API_URL }}
          target: production

      - name: Build Web Artists Docker image (GHA cache export only)
        if: ${{ !inputs.cache-to-registry }}
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./apps/web-artists/Dockerfile
          push: ${{ inputs.push-image }}
          tags: ${{ inputs.tags }}
          cache-from: |
            type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/cache/web-artists:buildcache
            type=gha,scope=web-artists
          cache-to: |
            type=gha,mode=max,scope=web-artists
          build-args: |
            VITE_API_URL=${{ env.BUILD_NEXT_PUBLIC_API_URL }}
          target: production
